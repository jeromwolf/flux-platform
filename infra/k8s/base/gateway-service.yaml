apiVersion: v1
kind: Service
metadata:
  name: imsp-gateway
  namespace: imsp
  labels:
    app: imsp-gateway
spec:
  type: ClusterIP
  selector:
    app: imsp-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
