apiVersion: v1
kind: Service
metadata:
  name: imsp-keycloak
  namespace: imsp
  labels:
    app: imsp-keycloak
spec:
  type: ClusterIP
  selector:
    app: imsp-keycloak
  ports:
    - name: http
      port: 8180
      targetPort: 8080
      protocol: TCP
